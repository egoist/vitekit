{
  "name": "@vitekit/adapter-node",
  "version": "0.0.1",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "prepublishOnly": "npm run build",
    "release": "release",
    "changelog": "echo skip"
  },
  "devDependencies": {
    "node-fetch": "^3.1.0",
    "@vitekit/server-utils": "workspace:*",
    "scripts": "workspace:*"
  }
}
